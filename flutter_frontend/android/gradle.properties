org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Disable configuration cache to avoid metadata.bin corruption (Kotlin DSL issue)
org.gradle.configuration-cache=false
# Disable Gradle daemon to prevent stale Kotlin compiler session errors
org.gradle.daemon=false
# Speed up builds with parallel execution
org.gradle.parallel=true
# Redirect Gradle cache to D:\Antigravity\.gradle to prevent C: drive cache corruption
gradle.user.home=D:/Antigravity/.gradle

